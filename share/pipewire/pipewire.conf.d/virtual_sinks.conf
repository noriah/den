context.objects = [
  # system mic
  {
    factory = adapter
    args = {
      factory.name = support.null-audio-sink
      node.name = "system_source"
      node.description = "Main System Mic"
      media.class = "Audio/Source/Virtual"
      audio.position = "FL,FR"
      monitor.passthrough = true
      monitor.channel-volumes = true
      adapter.auto-port-config = {
        mode dsp
        monitor = true
        position = preserve
      }
    }
  }

  # system mic mono
  {
    factory = adapter
    args = {
      factory.name = support.null-audio-sink
      node.name = "system_source_mono"
      node.description = "Main System Mic - Mono"
      media.class = "Audio/Source/Virtual"
      audio.position = "MONO"
      monitor.passthrough = true
      monitor.channel-volumes = true
      adapter.auto-port-config = {
        mode dsp
        monitor = true
        position = preserve
      }
    }
  }

  # guitar input
  {
    factory = adapter
    args = {
      factory.name = support.null-audio-sink
      node.name = "guitar_source"
      node.description = "Guitar Input"
      media.class = "Audio/Source/Virtual"
      audio.position = "MONO"
      monitor.passthrough = true
      monitor.channel-volumes = true
      adapter.auto-port-config = {
        mode dsp
        monitor = true
        position = preserve
      }
    }
  }

  # synth input
  {
    factory = adapter
    args = {
      factory.name = support.null-audio-sink
      node.name = "synth_source"
      node.description = "Synth Input"
      media.class = "Audio/Source/Virtual"
      audio.position = "FL,FR"
      monitor.passthrough = true
      monitor.channel-volumes = true
      adapter.auto-port-config = {
        mode dsp
        monitor = true
        position = preserve
      }
    }
  }

  # ext input
  {
    factory = adapter
    args = {
      factory.name = support.null-audio-sink
      node.name = "ext_source"
      node.description = "External Input"
      media.class = "Audio/Source/Virtual"
      audio.position = "AUX0,AUX1"
      monitor.passthrough = true
      monitor.channel-volumes = true
      adapter.auto-port-config = {
        mode dsp
        monitor = true
        position = preserve
      }
    }
  }

  # visualizer input
  {
    factory = adapter
    args = {
      factory.name = support.null-audio-sink
      node.name = "vis_source"
      node.description = "Visualizer Input"
      media.class = "Audio/Source/Virtual"
      audio.position = "FL,FR"
      monitor.passthrough = true
      monitor.channel-volumes = true
      adapter.auto-port-config = {
        mode dsp
        monitor = true
        position = preserve
      }
    }
  }

  # system output
  {
    factory = adapter
    args = {
      factory.name = support.null-audio-sink
      node.name = "system_sink"
      node.description = "System Output"
      media.class = "Audio/Sink"
      audio.position = "FL,FR"
      monitor.passthrough = true
      monitor.channel-volumes = true
      adapter.auto-port-config = {
        mode dsp
        monitor = true
        position = preserve
      }
    }
  }

  # media output
  {
    factory = adapter
    args = {
      factory.name = support.null-audio-sink
      node.name = "media_sink"
      node.description = "Media Output"
      media.class = "Audio/Sink"
      audio.position = "FL,FR"
      monitor.passthrough = true
      monitor.channel-volumes = true
      adapter.auto-port-config = {
        mode dsp
        monitor = true
        position = preserve
      }
    }
  }

  # voice output
  {
    factory = adapter
    args = {
      factory.name = support.null-audio-sink
      node.name = "voice_sink"
      node.description = "Voice Output"
      media.class = "Audio/Sink"
      audio.position = "FL,FR"
      monitor.passthrough = true
      monitor.channel-volumes = true
      adapter.auto-port-config = {
        mode dsp
        monitor = true
        position = preserve
      }
    }
  }

  # external output
  {
    factory = adapter
    args = {
      factory.name = support.null-audio-sink
      node.name = "ext_sink"
      node.description = "External Output"
      media.class = "Audio/Sink"
      audio.position = "AUX0,AUX1"
      monitor.passthrough = true
      monitor.channel-volumes = true
      adapter.auto-port-config = {
        mode dsp
        monitor = true
        position = preserve
      }
    }
  }

  # dummy output
  {
    factory = adapter
    args = {
      factory.name = support.null-audio-sink
      node.name = "dummy_null_sink"
      node.description = "Dummy Sink"
      media.class = "Audio/Sink"
      audio.position = "FL,FR"
      monitor.passthrough = true
      monitor.channel-volumes = true
      adapter.auto-port-config = {
        mode dsp
        monitor = true
        position = preserve
      }
    }
  }

  # {
  #   factory = link-factory
  #   args = {
  #     link.output.node = alsa_source.usb-Focusrite_Scarlett_18i20_USB_P9EVJ452789566-00.pro-input-0
  #     link.output.port = capture_AUX2
  #     link.input.node = system_source
  #     link.input.port = input_FR
  #     object.linger = truenn
  #   }
  # }
]
