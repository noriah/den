assign [instance="blueman-manager"] output $disp1
# Float the bluetooth management window
for_window [instance="blueman-manager"] floating enable

for_window [class="albert"] move position center

# random apps
# for_window [class="Visualizer" title="Visualizer"] floating enable
# for_window [class="Visualizer" title="Visualizer"] border none

# Spotify only on left monitor
# for_window [title="Spotify"] move to output $disp2
# for_window [title="Spotify"] border pixel $borderWidth

for_window [instance="1password"] floating enable

# chrome live caption
for_window [title="Live Caption"] floating enable

for_window [title="catnip" class="GLWindow"] floating enable

for_window [instance="cheese"] floating enable

for_window [instance="nomacs"] floating enable

# chat apps
# assign [class="discord"] $ws_comms
for_window [class="discord"] border pixel $borderWidth

# assign [class="Element"] $ws_comms

# assign [class="Signal"] $ws_comms

# assign [class="TelegramDesktop"] $ws_comms

# for_window[all] floating enable

for_window [class="steam"] floating enable

for_window [class="steamwebhelper"] floating enable
